dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-web'
    implementation(project(':components:crypto'))
    implementation(project(':components:support-advice-response'))
    implementation(project(':components:support-mustache'))
}